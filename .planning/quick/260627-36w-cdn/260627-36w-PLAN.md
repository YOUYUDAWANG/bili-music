---
quick_id: 260627-36w
slug: cdn
status: complete
created: 2026-06-26
---

# Quick Task 260627-36w: 设置里支持音频 CDN 测速并选择默认线路

## Goal

Allow users to manually test audio CDN candidates returned by Bilibili `playurl`, choose a preferred CDN host in Settings, and have playback prefer that host when it appears in fresh stream candidates.

## Tasks

1. Extend `AudioCDNSelector` with persisted preferred-host selection, manual candidate measurement, and deterministic ranking behavior.
2. Wire `BiliClient.audioStream` to preserve all returned candidates while selecting the preferred host when available.
3. Add a Settings UI for CDN speed testing, host selection, persisted probe rows, and clear fallback messaging.

## Verification

- Run focused unit tests for `AudioCDNSelectorTests`.
- Run the full `BiliMusicTests` unit target if the simulator is available.

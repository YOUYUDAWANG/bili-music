---
phase: "01-playback-critical-path-and-responsiveness"
plan: 4
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - "BiliMusic/Features/Home/HomeView.swift"
  - "BiliMusic/Player/RecommendationEngine.swift"
  - "BiliMusicTests/RecommendationSchedulingTests.swift"
  - "BiliMusicUITests/PlayerChromeUITests.swift"
autonomous: true
requirements:
  - RECO-01
  - RECO-04
must_haves:
  truths:
    - "User can start the first song after app launch without Home recommendations clearing, flashing, replacing, or auto-refreshing per D-09."
    - "Recommendation work is explicit, bounded, and lower priority than direct playback startup per D-08 and D-10."
  artifacts:
    - path: "BiliMusic/Features/Home/HomeView.swift"
      provides: "Stable visible Home recommendation list during first playback"
      contains: "load"
    - path: "BiliMusic/Player/RecommendationEngine.swift"
      provides: "Bounded home recommendation work policy"
      contains: "recommendations(mode:context:limit:)"
    - path: "BiliMusicTests/RecommendationSchedulingTests.swift"
      provides: "RECO-04 unit coverage"
    - path: "BiliMusicUITests/PlayerChromeUITests.swift"
      provides: "RECO-01 fixture UI coverage"
  key_links:
    - from: "BiliMusic/Features/Home/HomeView.swift"
      to: "BiliMusic/Player/RecommendationEngine.swift"
      via: "Home load/manual refresh asks for recommendations"
      pattern: "recommendations"
    - from: "BiliMusicUITests/PlayerChromeUITests.swift"
      to: "BiliMusic/Features/Home/HomeView.swift"
      via: "fixture taps homeTrackRow0 and asserts row remains visible"
      pattern: "homeTrackRow0"
---

## Phase Goal

**As a** user, **I want to** tapping a track, focusing search, and scrolling image-heavy music surfaces to stay responsive, **so that** the app reliably gets to music before background enrichment or recommendation work can interfere.

<objective>
Keep Home recommendations stable while playback starts.

Purpose: RECO-01, RECO-04, D-08, D-09, and D-10 require recommendations to stop competing with first playback and to avoid visible list churn as a playback side effect.
Output: Stable Home list behavior, explicit/low-priority recommendation scheduling, and unit/UI regression coverage.
</objective>

<execution_context>
@/Users/chaoyiliu/.codex/gsd-core/workflows/execute-plan.md
@/Users/chaoyiliu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-playback-critical-path-and-responsiveness/01-CONTEXT.md
@.planning/phases/01-playback-critical-path-and-responsiveness/01-RESEARCH.md
@.planning/phases/01-playback-critical-path-and-responsiveness/01-PATTERNS.md
@.planning/phases/01-playback-critical-path-and-responsiveness/01-UI-SPEC.md
@.planning/phases/01-playback-critical-path-and-responsiveness/01-VALIDATION.md
@BiliMusic/Features/Home/HomeView.swift
@BiliMusic/Player/RecommendationEngine.swift
@BiliMusic/Support/UITestFixtures.swift
@BiliMusicTests/RecommendationSchedulingTests.swift
@BiliMusicUITests/PlayerChromeUITests.swift
</context>

<artifacts_this_phase_produces>
- Home recommendation stability UI test for first playback.
- Recommendation scheduling tests for explicit refresh and bounded work.
- Narrow Home refresh state changes that keep visible rows stable.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve Home rows during first playback</name>
  <files>BiliMusic/Features/Home/HomeView.swift, BiliMusicUITests/PlayerChromeUITests.swift</files>
  <read_first>
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-CONTEXT.md`
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-PATTERNS.md`
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-UI-SPEC.md`
    - `BiliMusic/Features/Home/HomeView.swift`
    - `BiliMusic/Support/UITestFixtures.swift`
    - `BiliMusicUITests/PlayerChromeUITests.swift`
  </read_first>
  <behavior>
    - Test 1: In fixture mode, tapping `homeTrackRow0` shows the mini-player and leaves the same Home row visible at the same approximate vertical position per D-09.
    - Test 2: A failed Home refresh with existing rows leaves those rows visible and surfaces error text without replacing the list with blank state.
    - Test 3: Starting playback may mark Home data stale internally but does not call Home `load()` as a playback side effect.
  </behavior>
  <action>Adjust `HomeView` refresh state so visible `tracks` are not cleared before a replacement batch is ready. Keep refresh and `换一批` as explicit user/surface actions. If playback changes make Home data stale, store that as internal state only and do not auto-call `load()` from the playback path. Extend `PlayerChromeUITests` fixture coverage to tap a Home row, wait for `miniPlayer`, and assert the row remains visible without list jump per the UI-SPEC.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project BiliMusic.xcodeproj -scheme BiliMusic -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:BiliMusicUITests/PlayerChromeUITests CODE_SIGNING_ALLOWED=NO</automated>
  </verify>
  <acceptance_criteria>
    - Home rows are not set to an empty array at refresh start when existing rows are visible.
    - First playback from a Home row does not visually clear, flash, replace, or reset the Home list.
    - Refresh failure with existing rows keeps rows visible and shows non-blocking error copy.
    - UI test fixture coverage proves `homeTrackRow0` remains visible after tap-to-play.
  </acceptance_criteria>
  <done>RECO-01 Home list stability is observable and regression-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound Home recommendation work below playback priority</name>
  <files>BiliMusic/Player/RecommendationEngine.swift, BiliMusic/Features/Home/HomeView.swift, BiliMusicTests/RecommendationSchedulingTests.swift</files>
  <read_first>
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-CONTEXT.md`
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-RESEARCH.md`
    - `.planning/phases/01-playback-critical-path-and-responsiveness/01-PATTERNS.md`
    - `.planning/codebase/CONCERNS.md`
    - `BiliMusic/Player/RecommendationEngine.swift`
    - `BiliMusic/Features/Home/HomeView.swift`
    - `BiliMusicTests/RecommendationSchedulingTests.swift`
  </read_first>
  <behavior>
    - Test 1: Home recommendation work starts only from Home surface load/manual refresh paths and not from `PlayerEngine.play`.
    - Test 2: Home recommendation candidate work has explicit seed/page/request bounds and lower task priority than direct playback startup per D-10.
    - Test 3: Radio next-track behavior remains callable and is not blocked by Home refresh state.
  </behavior>
  <action>Add a small recommendation scheduling policy at the Home/RecommendationEngine boundary that names the trigger (`initialHomeLoad`, `manualRefresh`, or equivalent) and carries explicit bounds for seed count, per-seed limit, and task priority. Apply it only to Home recommendation calls in Phase 1. Keep radio and related panel behavior intact unless needed to preserve existing compilation. Update tests to prove Home work is explicit, bounded, and not reachable from direct playback startup.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project BiliMusic.xcodeproj -scheme BiliMusic -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:BiliMusicTests/RecommendationSchedulingTests CODE_SIGNING_ALLOWED=NO</automated>
  </verify>
  <acceptance_criteria>
    - `RecommendationSchedulingTests` proves no Home recommendation refresh is triggered by direct playback startup.
    - Home recommendation calls include explicit request bounds and run below direct playback priority.
    - Existing radio next-track API still compiles and remains covered by any existing caller expectations.
    - No broad BiliClient, auth, favorite, or cache rewrite is introduced.
  </acceptance_criteria>
  <done>RECO-04 recommendation work is bounded and lower priority than playback startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home recommendation refresh -> Bilibili APIs | Refresh work can fan out to related/search/favorite endpoints. |
| Playback tap -> Home state | A direct playback action could mutate visible recommendation state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Denial of Service | `RecommendationEngine` | mitigate | Use explicit request bounds and lower-priority scheduling for Home refresh work. |
| T-01-10 | Tampering | `HomeView` visible rows | mitigate | Do not clear or replace visible rows as a playback side effect; UI tests assert row stability after tap. |
| T-01-11 | Information Disclosure | recommendation network requests | mitigate | No new diagnostic payloads or account data logging are added; Bilibili requests stay behind existing `BiliClient` boundaries. |
| T-01-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Run focused recommendation tests and UI fixture test, then the full suite:
`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project BiliMusic.xcodeproj -scheme BiliMusic -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGNING_ALLOWED=NO`
</verification>

<success_criteria>
- First playback from Home does not clear, reset, flash, or replace visible recommendations.
- Home recommendation work starts only from explicit Home surface actions.
- Recommendation work is bounded and lower priority than direct playback startup.
</success_criteria>

<output>
Create `.planning/phases/01-playback-critical-path-and-responsiveness/01-04-SUMMARY.md` when done.
</output>
